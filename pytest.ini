[pytest]
asyncio_mode = auto

markers =
    integration: marks tests that require external systems or credentials
    asyncio: marks tests as async (deselect with '-m "not asyncio"')
    real_pipeline: marks tests that require a live Kafka pipeline with real data
